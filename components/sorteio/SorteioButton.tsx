import { TouchableOpacity, Text } from 'react-native';

interface SorteioButtonProps {
  // Definir props aqui
}

export default function SorteioButton(props: SorteioButtonProps) {
  return (
    <TouchableOpacity>
      <Text>Sorteio Button</Text>
    </TouchableOpacity>
  );
}